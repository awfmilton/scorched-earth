<!-- generated by MCP Config Manager (skill library) — edits are overwritten on backfill -->
# SKILLS.md — scorched-earth

Verified skills from the MCP Config Manager skill library that reference this
project. Each is a proven recipe extracted from a successful run and admitted
through the sandboxed surrogate verifier — follow it instead of re-deriving.
Query the live library with the `skill_search` / `skill_get` MCP tools.

_No project-specific verified skills yet. They appear here automatically after verified runs are compiled and admitted._
